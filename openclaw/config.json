{
  "llm": {
    "provider": "openai-compatible",
    "baseUrl": "http://vllm:8000/v1",
    "apiKey": "EMPTY",
    "_apiKey_note": "Set to match VLLM_API_KEY in .env. 'EMPTY' (or any value) works when vLLM runs in no-auth mode (VLLM_API_KEY unset/EMPTY). When VLLM_API_KEY is a real secret, set apiKey to that same value so OpenClaw sends 'Authorization: Bearer <key>'.",
    "model": "Qwen3.5-9B-Q4_K_M",
    "maxTokens": 4096,
    "temperature": 0.7
  },
  "server": {
    "host": "0.0.0.0",
    "port": 18789
  }
}
